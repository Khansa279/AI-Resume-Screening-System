# Core dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0

# Document parsing
PyPDF2>=3.0.0
python-docx>=1.1.0

# Data validation and configuration
pydantic>=2.0.0
python-dotenv>=1.0.0

# Persistence (src/db) -- was missing from this file even though
# src/db/database.py has always imported it directly.
SQLAlchemy>=2.0.0

# CLI
rich>=13.0.0

# API layer (FastAPI production backend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0